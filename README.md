INSERT INTO T_IOPP_PROC_RAW (DATE_TIME, TAG_NAME, TAG_VALUE)
SELECT 
    DATE_TIME,
    DBMS_LOB.SUBSTR(TAG_NAME, 4000, 1) AS TAG_NAME,
    TAG_VALUE
FROM dbo.TAG_LOGS@IOPP
WHERE DATE_TIME >= TO_DATE('09-DEC-2025','DD-MON-YYYY');