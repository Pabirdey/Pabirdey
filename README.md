title: {
    text: "📊 " + element + " - " + type.replaceAll("_", " "),
    subtext: "Last 30 Days Trend",
    left: 'center',

    textStyle: {
        color: '#0d6efd',
        fontSize: 20,
        fontWeight: 'bold',
        fontFamily: 'Segoe UI',
        letterSpacing: 1
    },

    subtextStyle: {
        color: '#666',
        fontSize: 13,
        fontStyle: 'italic'
    }
}
