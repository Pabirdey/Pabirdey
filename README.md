 int FurFaceValue = null;
                            if (maturityPerc== 0)
                            {
                                FurFaceValue = 0.5;
                                else if (totalRunningHr > 1450 ||( FurCircuit== 'G' && FurCircuit == 'H' && FurCircuit= = 'I'))
                                {
                                    FurFaceValue =1;
                                }
                                else
                                    FurFaceValue = 0;
                            }
